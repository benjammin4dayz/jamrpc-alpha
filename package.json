{
  "name": "jamrpc-alpha",
  "prettyName": "JamRPC Alpha",
  "version": "0.3.0",
  "private": true,
  "description": "",
  "main": "",
  "scripts": {
    "start": "neu run",
    "build": "node build-scripts && neu build",
    "clean": "rm -rf dist/ node_modules/ && npm run react-src:tidy",
    "app-src:build": "npm run build --workspace ./app-src/node",
    "react-src:tidy": "cd react-src && rm -rf build/ node_modules/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "fix": "prettier --write '**/*.{js,jsx,ts,tsx,json}'"
  },
  "author": "benjammin4dayz",
  "license": "MIT",
  "workspaces": [
    "extensions/",
    "react-src/"
  ],
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "prettier-eslint": "^16.3.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "singleQuote": true
  },
  "dependencies": {
    "discord-presence-utils": "github:benjammin4dayz/discord-presence-utils#v2.2.0"
  }
}
